# 09-测试方案与测试记录

## 1. 测试目标
- 验证认证链路与权限拦截可用。
- 验证数据范围控制不越权。
- 验证报修状态机流转合法性。
- 验证阶段5新增日志查询接口可用。

## 2. 自动化测试清单（已落地）
- `AuthServiceImplTest`
- `DataScopeServiceTest`
- `RepairStateMachineTest`

已覆盖重点：
- 登录成功/失败校验。
- `ALL` 与 `PROPERTY_COMPANY -> COMPLEX` 数据范围展开。
- 报修合法流转、非法流转拦截与业务异常抛出。

## 3. 执行记录（2026-03-24）
- JDK：`Z:\jdk21`
- Maven：`3.9.3`
- 执行命令：`mvn test`
- 结果：

| 指标 | 数值 |
|---|---|
| Tests run | 6 |
| Failures | 0 |
| Errors | 0 |
| Skipped | 0 |

## 4. 手工联调用例（阶段5）
| 编号 | 用例 | 步骤 | 预期结果 |
|---|---|---|---|
| T-LG-01 | 登录日志查询 | `GET /api/logs/logins?current=1&size=10` | 返回登录日志分页数据 |
| T-LG-02 | 操作日志查询 | `GET /api/logs/operations?operationModule=报修` | 返回报修模块操作日志 |
| T-LG-03 | 日志权限拦截 | 无 `log:*` 权限账号访问日志接口 | 返回统一无权限错误 |
| T-RP-01 | 报修创建 | 居民调用创建报修接口 | 状态为 `PENDING_ACCEPT` |
| T-RP-02 | 报修主流程 | 受理 -> 分派 -> 接单 -> 提交结果 -> 居民确认 | 状态按规则推进 |
| T-RP-03 | 非法流转 | 在 `PENDING_ACCEPT` 直接提交处理结果 | 返回 `ILLEGAL_STATE_FLOW` |

## 5. 后续测试补强建议
- 为日志查询接口增加 MockMvc 测试，覆盖分页边界与参数校验。
- 为报修服务增加更多越权路径自动化测试。
- 增加活动报名并发压测记录，沉淀性能基线报告。
