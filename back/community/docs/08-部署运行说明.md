# 08-部署运行说明

## 1. 环境要求
- JDK 17+
- Maven 3.9+
- MySQL 8.x
- Redis 6.x+

## 2. 目录说明
- 后端工程：`back/community`
- SQL 脚本：`back/community/sql`
- 技术文档：`back/community/docs`

## 3. 数据库初始化（手动执行）
在 MySQL 中创建数据库（建议名：`community_service`），并按顺序执行：
1. `sql/01_schema.sql`
2. `sql/02_index.sql`
3. `sql/03_init_data.sql`

说明：
- 项目不强依赖 Flyway/Liquibase。
- 采用手工 SQL 初始化，便于毕业设计答辩演示与版本回溯。

## 4. 配置文件
- 主配置：`src/main/resources/application.yml`
- 开发环境：`src/main/resources/application-dev.yml`
- 生产环境：`src/main/resources/application-prod.yml`
- 本地示例：`src/main/resources/application-local.yml.example`

建议通过环境变量注入敏感配置：
- `COMMUNITY_DB_URL`
- `COMMUNITY_DB_USERNAME`
- `COMMUNITY_DB_PASSWORD`
- `COMMUNITY_REDIS_HOST`
- `COMMUNITY_REDIS_PORT`
- `COMMUNITY_REDIS_PASSWORD`
- `COMMUNITY_JWT_SECRET`
- `COMMUNITY_STORAGE_TYPE`（可选，数据库无配置时的默认值）
- `COMMUNITY_LOCAL_STORAGE_PATH`（可选，本地存储根目录）

## 5. 本地启动
```bash
mvn clean spring-boot:run -Dspring-boot.run.profiles=dev
```

默认访问：
- 服务地址：`http://localhost:8080`
- Swagger UI：`http://localhost:8080/swagger-ui/index.html`
- Knife4j：`http://localhost:8080/doc.html`

## 6. 默认管理员
- 用户名：`admin`
- 初始密码：`Admin@123456`
- 首次登录强制改密：`must_change_password=1`

## 7. 本地联调建议顺序
1. 执行 SQL 脚本并确认表、索引、初始数据存在。
2. 启动 Redis。
3. 启动应用后先验证居民认证链路：
- `POST /api/auth/resident/register`
- `POST /api/auth/password/reset-code`
- `POST /api/auth/password/reset-by-code`
- `POST /api/auth/login`
4. 再验证管理员链路与业务模块：系统管理、组织、公告、活动、报修、日志查询。

## 9. 文件上传与存储切换
### 9.1 本地存储（默认）
- 初始化数据已写入 `sys_storage_config` 默认值 `LOCAL`。
- 上传接口：`POST /api/files/upload`。
- 本地文件目录默认 `./storage`，可通过 `COMMUNITY_LOCAL_STORAGE_PATH` 覆盖。

### 9.2 七牛云存储切换
1. 使用超级管理员登录。
2. 调用 `PUT /api/system/storage-config`，设置：
- `storageType=QINIU`
- `qiniuAccessKey`
- `qiniuSecretKey`
- `qiniuBucket`
- `qiniuRegion`（如 `z0/z1/z2/na0/as0/auto`）
- `qiniuDomain`（用于拼接访问地址）
3. 切换后新上传文件自动走七牛云，历史本地文件仍可下载。

## 8. 常见问题
- MySQL 连接失败：检查数据库地址、用户名、密码、字符集和时区参数。
- Redis 连接失败：检查服务是否启动、密码与端口是否正确。
- 接口返回 401：确认携带 `Authorization: Bearer <token>`。
- 接口返回 403：确认当前角色已授予对应权限点。
- 登录后提示必须改密：属于系统预置管理员安全策略，改密后可继续操作。
